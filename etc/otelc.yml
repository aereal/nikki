---
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
exporters:
  otlp:
    endpoint: "${env:JAEGER_HOST}"
    tls:
      insecure: true
service:
  pipelines:
    traces:
      receivers:
        - otlp
      exporters:
        - otlp
