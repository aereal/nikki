/**
 * @type {import('prettier').Config}
 */
const config = {
  arrowParens: 'always',
  bracketSpacing: true,
  jsxSingleQuote: true,
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
}

export default config
